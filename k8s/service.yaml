apiVersion: v1
kind: Service
metadata:
  name: studio-service
  labels:
    app: studio
spec:
  type: NodePort
  selector:
    app: studio
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
